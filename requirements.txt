# Runtime dependencies for the Streamlit demo — kept minimal for fast Streamlit Cloud builds.
# To run the notebook end-to-end (incl. the neural-network section), use requirements-notebook.txt.
streamlit
scikit-learn==1.7.2
xgboost==3.2.0
joblib>=1.3
pandas>=2.0
numpy>=1.26,<3
